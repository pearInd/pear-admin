# dependencies
node_modules/

# secrets — NEVER commit the real Decart key
.env
.env.local

# scratch dirs from image/gate checks
.tmp_*/

# logs / os
npm-debug.log*
*.log
.DS_Store
Thumbs.db
.vercel

# local session store (test data — not committed)
sessions.json
data/
.env*
